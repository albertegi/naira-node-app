spring:
  data:
    mongodb:
      username: albert
      password: albert
      host: localhost
      port: 27017
      database: customer
      authentication-database: admin

  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: paymentGroup, orderGroup
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringSerializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.trusted.packages: '*'
        spring.json.type.mapping: orderConfirmation:com.alvirg.ecommerce.kafka.OrderConfirmation,paymentConfirmation:com.alvirg.ecommerce.notification.PaymentNotificationRequest

  mail:
    host: localhost
    port: 1025
    username: albert
    password: albert


    properties:
      mail:
        smtp: '*'
        auth: true
        starttls:
          enabled: true
          connectiontimeout: 5000
          timout: 3000
          writetimeout: 5000

server:
  port: 8040
